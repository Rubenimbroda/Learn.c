#include <stdio.h>

int main() {
    int a = 914;
    float b = 18;
    double c = 19;
    float sum;      // creo las variables para poder sumarlas //

    sum = a + b + c; // creo la función para sumar los numeros //

    printf("The sum of a, b, and c is %f", sum); // imprimo la suma //
    return 0;
}